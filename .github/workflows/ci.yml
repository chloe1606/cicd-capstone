name: CI
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: build-step                                         # gives this step an addressable name
        run: ./scripts/build.sh                            # the script writes "tarball=..." to $GITHUB_OUTPUT
      - uses: actions/upload-artifact@v4
        with:
          name: pipeline-bundle
          path: ./dist

  deploy-dev:
    needs: build
    environment: dev
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v5
        with:
          name: pipeline-bundle
      - run: ./scripts/deploy.sh dev

  deploy-test:
    needs: deploy-dev
    environment: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v5
        with:
          name: pipeline-bundle
      - run: ./scripts/deploy.sh test

  deploy-prod:
    needs: deploy-test
    environment: prod
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v5
        with:
          name: pipeline-bundle
      - run: ./scripts/deploy.sh prod